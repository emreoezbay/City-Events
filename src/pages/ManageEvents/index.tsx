import React from 'react';
import { Typography } from '@mui/material';

type Props = {};

export default function ManageEvents({}: Props) {
  return <Typography variant="h1">ManageEvents</Typography>;
}
